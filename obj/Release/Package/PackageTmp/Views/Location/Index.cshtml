@model IEnumerable<TauseelRestaurant.Models.Location_T>
<script src="~/js/jquery-1.8.3.min.js"></script>
<div class="container-fluid">
    <div class="page-content">
        <div class="row" style="min-height:850px;">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading clearfix">
                        <script>
                            $(document).ready(function () {
                                $(".editrow").hide();
                            });
                            function openrow(row) {
                                $("#edit-" + row).show(2000);

                            }
                        </script>



                        <div id="addform" style="width:100%;">

                            @using (Html.BeginForm("Create", "Location"))
                            {
                                @Html.AntiForgeryToken()
                                @Html.ValidationSummary(true)
                                

                                <div class="col-md-8">

                                    <input type="text" name="Location" class="form-control cke_required" placeholder="Location" />
                                </div>
                                <div class="col-md-4">
                                    <input type="submit" name="submit" class="btn btn-success" value="Save" />
                                </div>

                            }
                        </div>
                        </header>
                        <table class="table table-striped table-advance table-hover">
                            <thead>
                                <tr>
                                    <th><i class="fa fa-sort"></i> Location</th>
                                    <th>Modify</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @item.Location
                                        </td>


                                        <td>

                                            <button onclick="openrow(@item.LocationID)" class="btn btn-primary"><i class="fa fa-pencil"></i> Edit</button>

                                        </td>
                                    </tr>
                                    using (Html.BeginForm("Edit", "Location"))
                                    {
                                        @Html.AntiForgeryToken()
                                        @Html.ValidationSummary(true)
                                        <tr id="edit-@item.LocationID" class="editrow">
                                            <td>
                                                <input class="form-control cke_required" type="text" name="Location" value="@item.Location" />
                                                <input type="hidden" name="LocationID" value="@item.LocationID" />
                                            </td>
                                            <td>

                                                <button class="btn btn-danger"><i class="fa fa-check"></i> Update</button>

                                            </td>
                                        </tr>
                                    }

                                }
                            </tbody>
                        </table>
</section>
            </div>



        </div>
    </div>
</div>


